package com.kh.start.auth.controller;

import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import com.kh.start.auth.model.service.AuthService;
import com.kh.start.member.model.dto.MemberDTO;

import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;

@Slf4j
@RestController //JSON방식로 할거임 즉 객체 방식
@RequestMapping("auth")
@RequiredArgsConstructor// 필요한 생성자를 만들어줌 
public class AuthController {
	private final AuthService authService;
	
	@PostMapping("/login")
	public ResponseEntity<?> login (MemberDTO member){
		
		authService.login(member);
		//로그인 방식=> 웹 애플리케이션은 대부분 인증과정이 포함됨
		
		
		
		
		return null;
	}
	
	
	
	

}
